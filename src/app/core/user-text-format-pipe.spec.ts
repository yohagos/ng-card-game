import { UserTextFormatPipe } from './user-text-format-pipe';

describe('UserTextFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new UserTextFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
